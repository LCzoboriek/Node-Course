var user = {
    id: 1000,
    name: 'fred',
    age: 32,
    cloth_size: ['large', 10, 'medium']
};
// This is a very simple object, you can put lots of different things in objects, you can add arrays, etc as well

console.log(`User: ${user.name} is aged ${user.age} and cloth size is ${user.cloth_size}`);